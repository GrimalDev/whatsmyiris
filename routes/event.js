import express from "express";
const router = express.Router();

//dotenv config
import dotenv from "dotenv";
dotenv.config();

/* GET home page. */
router.get('/', async function (req, res, next) {
    //return the page to add an event

});

export default router;